# 每日积分打卡需求记录

## 规则保存与云同步

- 用户在「规则」页新增或修改规则并点击保存后，规则必须立即写入本地存储。
- 已保存的本地规则不能被云同步初始化阶段返回的旧云端规则覆盖。
- 云端同步规则时，需要携带规则自身的更新时间；多设备合并时以规则更新时间判断是否替换本地规则。
- 旧版本云端文档如果没有规则更新时间，而本地已经由新版保存过规则，应保留本地规则并回传到云端。

## 验收

- 新增自定义加分项，保存后刷新页面仍可看到。
- 在云端返回旧规则的情况下，本地刚保存的规则仍保留，并会再次上传到云端。

## 数学竞赛获奖加分

- 一等奖固定加 30 分。
- 状元奖固定加 100 分。
- 其他奖不设固定分值，在「今天」页选择“其他奖”后手动填写本次加分。
- 旧版保存过的“特等奖/金奖/状元”分值配置加载后应自动规范为上述新规则。
